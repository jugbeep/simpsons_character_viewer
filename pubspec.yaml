name: simpsons_character_viewer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  mobx: ^2.0.6+1
  flutter_mobx: ^2.0.4
  badges: ^2.0.2
  retrofit: ^4.0.1
  dio: ^5.1.1
  json_annotation: ^4.8.1
  flutter_html: ^2.2.1
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.4.1
  flutter_lints: ^1.0.0
  mobx_codegen: ^2.0.5+2
  build_runner: ^2.3.3
  retrofit_generator: ^5.0.0
  json_serializable: ^6.6.2

flutter:
  uses-material-design: true
  assets:
    - images/Simpsonswave.gif
